export interface EventDetails {
  title: string;
  subtitle: string;
  theme: string;
  date: Date;
  location: string;
  locationDetails: string;
}

export interface TimeLeft {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}
